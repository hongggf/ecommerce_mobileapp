import 'package:flutter/material.dart';

class AppColors {
  // **Brand Colors (Main Identity)**
  static const primary = Color(0xFFD4A056);     // Soft Luxe Gold
  static const secondary = Color(0xFF1F2533);   // Charcoal Navy Deep
  static const accent = Color(0xFF8E7DFF);      // Modern Purple Accent

  // **Status Colors**
  static const success = Color(0xFF2ECC71);     // Fresh Green
  static const warning = Color(0xFFF1C40F);     // Soft Yellow Warning
  static const error = Color(0xFFE74C3C);       // Clean Red

    // Light Theme Colors
  static const lightBackground = Color(0xFFFFFFFF);
  static const lightSurface = Color(0xFFF5F5F5);
  static const lightTextPrimary = Color(0xFF212121);
  static const lightTextSecondary = Color(0xFF757575);

  // Dark Theme Colors
  static const darkBackground = Color(0xFF121212);
  static const darkSurface = Color(0xFF1E1E1E);
  static const darkTextPrimary = Color(0xFFFFFFFF);
  static const darkTextSecondary = Color(0xFFB0B0B0);
}
