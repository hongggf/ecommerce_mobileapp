import 'package:flutter/material.dart';

class UserCheckoutView extends StatelessWidget {
  const UserCheckoutView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [

        ],
      ),
    );
  }
}
