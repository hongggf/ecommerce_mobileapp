import 'package:flutter/widgets.dart';

class ButtomNavController extends StatelessWidget {
  const ButtomNavController({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}